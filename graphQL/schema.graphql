type Movie {
  id: Int!
  name: String!
  score: Int!
}

type Query {
  movies: [Movie]! # 여러 Movie 들의 배열
  movie(id: Int!): Movie # 특정 id의 movie을 찾아 Movie 를 리턴
}

type Mutation {
  addMovie(name: String!, score: Int!): Movie!
}
